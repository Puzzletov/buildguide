# Environments

- `development`: local machine and local env vars in `.env.local`.
- `staging`: preview/staging deployment with separate Supabase project.
- `production`: main branch deployment with production Supabase project.

